spring:
  config:
    import:
      - optional:file:env/spring.env[.properties]
  profiles:
    active: dev
    group:
      dev: dev, common
      blue: blue, common
      green: green, common
server:
  env: blue